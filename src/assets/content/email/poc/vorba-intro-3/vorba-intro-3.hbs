<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{#if recipientName}}Vorba Discovery Email - {{recipientName}}{{else}}Vorba Discovery Email Content{{/if}}</title>
  </head>
  <body style="margin:0; padding:0; background-color:#f4f4f7;">
    <table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="background-color:#f4f4f7; padding:24px 0;">
      <tr>
        <td align="center">
          <table role="presentation" width="600" cellspacing="0" cellpadding="0" border="0" style="background-color:#ffffff; border-radius:8px; padding:32px; font-family:Arial, sans-serif; color:#1f2933;">

            <!-- Title -->
            <tr>
              <td style="font-size:24px; font-weight:bold; line-height:1.3; padding-bottom:16px;">
                {{#if recipientName}}
                  Hello {{recipientName}},
                {{else}}
                  Hello,
                {{/if}}
              </td>
            </tr>

            <!-- Body content -->
            <tr>
                <td style="font-size:16px; line-height:1.5; padding-bottom:24px;">
                    <strong>Vorba modernizes the systems your business depends on.</strong>
                    <br/><br/>
                    We stabilize what's brittle, upgrade what's outdated, and rebuild foundations so your teams can move faster, ship confidently, and grow without technical drag.
                    <br/><br/>
                    {{#if customQuestion}}
                      {{customQuestion}}
                    {{else}}
                      Are your core systems slowing you down?
                    {{/if}}
                </td>
            </tr>

            <!-- CTA link -->
            <tr>
              <td style="padding-top:8px; padding-bottom:8px;">
                <a href="{{#if ctaUrl}}{{ctaUrl}}{{else}}https://vorba.com/{{/if}}{{#if utmParams}}?{{utmParams}}{{/if}}"
                   style="font-size:16px; color:#2563eb; text-decoration:none; font-weight:bold;">
                  Discover our value &gt;
                </a>
              </td>
            </tr>

            {{#if senderName}}
            <!-- Personalized footer -->
            <tr>
              <td style="font-size:14px; color:#6b7280; padding-top:24px; border-top:1px solid #e2e8f0;">
                Best regards,<br/>
                {{senderName}}<br/>
                {{#if senderTitle}}{{senderTitle}}<br/>{{/if}}
                {{#if companyName}}{{companyName}}{{/if}}
              </td>
            </tr>
            {{/if}}

          </table>
        </td>
      </tr>
    </table>
  </body>
</html>